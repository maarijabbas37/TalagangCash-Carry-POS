export function Dashboard() {
  // Placeholder for the POS billing screen — built in Phase 2.
  return (
    <div className="p-8">
      <h2 className="text-lg font-semibold mb-2">POS</h2>
      <p className="text-gray-500 text-sm">
        Billing screen arrives in Phase 2. Phase 1 covers login, roles, counters,
        and the product master (see the Products tab).
      </p>
    </div>
  );
}
